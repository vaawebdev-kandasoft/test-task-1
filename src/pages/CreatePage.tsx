/**
 * @todo
 * This page must present a form to create post.
 * Using jsonplaceholder API setup a form to create post with title and body.
 * While user submits the form all inputs & submit button must be disabled.
 * Form must have a cancel functionality that leads to index page.
 *
 * @see https://jsonplaceholder.typicode.com
 */
export default function CreatePage() {
  return <>Create Page</>;
}
