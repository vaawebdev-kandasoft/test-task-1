/**
 * @todo
 * This page must present a paginated table of posts with 10 records per page
 * Table must include id, title and body.
 * Pagination is client-side, it must include next & previous page navigation.
 * This page must show a loading state while fetching the API.
 * This page must have a link button that leads to the create page.
 * Use jsonplaceholder API setup an ajax call to retrieve posts.
 *
 * @see https://jsonplaceholder.typicode.com
 */
export default function IndexPage() {
  return <>Index Page</>;
}
